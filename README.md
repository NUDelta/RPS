# A Sprint Planning System for Researchers

### Setup
To run this project...
- Clone the repo
- `cd` in the repo's directory, and run `npm install`
- Start the server by running `webpack-dev-server`
- Open your web browser to localhost:8080
